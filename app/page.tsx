/**
 * Root Entry Page
 * 
 * Redirects or embeds the primary Dashboard as the default view.
 */
import DashboardPage from './dashboard/page';

export default function Home() {
  return <DashboardPage />;
}
